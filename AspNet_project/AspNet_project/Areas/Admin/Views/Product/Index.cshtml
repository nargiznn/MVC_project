@model IEnumerable<ProductVM>

<div class="container mt-5">
    <h1 class="text-center">Products</h1>
    <div class="create-area">
        <a asp-action="Create" class="btn btn-success">Create</a>
    </div>
    <div class="table-responsive pt-3 mt-3">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>
                        Id
                    </th>
                    <th>
                        Image
                    </th>
                    <th>
                        Title
                    </th>
                    <th>
                        CategoryName
                    </th>
                    <th>
                        Info
                    </th>

                    <th>
                        Price
                    </th>
                    <th>
                        DiscountPrice
                    </th>
                    <th>
                        SalesCount
                    </th>

                    <th>
                        Options
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>
                            <img src="~/assets/img/@item.MainImage" style="width:100px;height:80px" alt="image">
                        </td>
                        <td>
                            @item.Title
                        </td>
                        <td>
                            @item.CategoryName
                        </td>
                        <td>
                            @item.Info
                        </td>
                        <td>
                            @item.Price
                        </td>
                        <td>
                            @item.DiscountPrice
                        </td>
                        <td>
                            @item.SalesCount
                        </td>
                        <td>
                            <a asp-action="Detail" asp-route-id="@item.Id" class="btn btn-outline-primary">Detail</a>
                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-secondary">Edit</a>
                            <form asp-action="Delete" asp-route-id="@item.Id" method="post" class="d-inline">
                                <button type="submit" class="btn btn-outline-danger">Delete</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>